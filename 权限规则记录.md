# 权限规则记录

本文件记录项目中需要长期遵守的权限规则。以后改权限前优先看这里，避免重复从代码里猜。

## 总原则

- 平台总后台只认 `user.id == 555`，不认权限码。
- 权限码 `555` 是公司层面的管理员权限，不等于平台总管理员。
- 公司模块权限和用户权限是两层判断：
  - 公司没有开模块：显示“当前公司未开通xxx，请联系平台管理员授权。”
  - 公司开了模块但用户没权限：显示“当前用户没有xxx权限，请询问公司管理员。”
- 菜单入口按公司模块显示；用户没有对应权限时，进入页面/API 后显示用户无权限，不要误提示公司未授权。

## 平台总后台

- 路径：`/system-admin/`
- API：`/api/system-admin/*`
- 规则：仅 `user.id == 555` 可访问。
- 注意：`code=555` 的公司管理员不能访问平台总后台。

## 公司模块

当前公司模块：

- `order_fulfillment`：导单发货功能
- `automation`：自动化功能
- `logistics`：物流板块
- `theme`：主题板块
- `infringement`：侵权板块

模块与用户权限码：

- `logistics`：`5`、`6`、`557`、`555`
- `theme`：`8`、`555`
- `infringement`：`556`、`555`

## 主题/侵权特殊规则

- 主题板块页面和内部 API 通常需要：
  - 公司开通 `theme`
  - 用户拥有 `8` 或 `555`
- 侵权板块页面和内部 API 通常需要：
  - 公司开通 `infringement`
  - 用户拥有 `556` 或 `555`
- `D:\Y-Project\Overlord\theme\view\views_trend.py` 是例外：
  - `trend_search` / `/api/trend/search/` 是外部调用接口，保持公开。
  - 不要给 `views_trend.py` 的外部查询接口加 `module_access_required` 或 `theme_access_required`。
  - 如果以后需要限制，只能新增独立内部接口，不要改这个公开接口。

## 物流板块

- 页面：`/tracking-management/`
- API：`/api/tracking-*`、`/api/import-tracking-excel/`、`/api/export-tracking-excel/` 等物流管理接口
- 规则：
  - 公司开通 `logistics`
  - 用户拥有物流相关权限码：`5`、`6`、`557`、`555`
- 物流数据需要按公司隔离。

## 仅 user.id == 555

以下功能只给账号主键 `555` 使用：

- 平台总后台：`/system-admin/`
- 外部采购管理：`/external-procurement-management/`
- 产品需求：`/task/product/*`、`/api/products/*`
- 数据部需求进度：`/data_req/*`

## 公开/外部调用接口

以下接口是外部调用或公开接口，不要随手加登录或模块权限：

- `/amazon/api/*`：影刀/巡店等外部调用
- `/api/trend/search/`：侵权趋势/词库外部查询，位于 `theme/view/views_trend.py`
- `/api/external/tasks/*`
- `/api/external/approval/*`
- `/api/external/amazon-shops/*`
- `/api/external/amazon-products*`：需要由 view 自身校验 `X-RPA-Secret`
- `/api/external/divi-account/*`
- `/api/rpa/*`：中间件要求 `X-RPA-Secret`

## 数据隔离

- 公司与公司之间的数据必须隔离。
- 系统级数据只给 `user.id == 555`。
- 公司用户只能看到自己公司范围内的数据。
- 店铺、运营人员、需求、物流等业务数据都要优先检查是否已有公司字段或可追溯公司关系。

## 维护约定

- 每次新增权限规则、公开接口、只给 555 的页面，都要同步更新本文件。
- 自动扫描出来的完整 API 清单放在 `API权限清单.md`，但最终特殊规则以本文件为准。
