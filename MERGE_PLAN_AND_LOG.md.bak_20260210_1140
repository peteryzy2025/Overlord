# MERGE_PLAN_AND_LOG

## 0. 基线信息
- 启动时间: 2026-02-09
- 工作分支: `merge-test`
- 策略: 以 `origin/master` 为主干，逐页面验收，模型/后端冲突采用双方能力并存

## 1. 目标
- 保留同事的新架构与新样式
- 保留我方筛选框、筛选弹窗、按钮样式与交互逻辑大改
- 避免仅依赖 git 自动冲突，覆盖自动合并但可能错乱的页面

## 2. 高风险共同修改文件（强制人工复核）
1. `amazon/models.py`
2. `amazon/templates/amazon_daily_check_report.html`
3. `amazon/templates/amazon_order_management.html`
4. `amazon/templates/amazon_performance_notifications.html`
5. `amazon/templates/amazon_shop_emails.html`
6. `amazon/templates/dashboard.html`
7. `amazon/templates/ranking.html`
8. `amazon/view/views_amazon_daily_check.py`
9. `data_req/templates/data_req/requirement_list.html`
10. `general/templates/base/base.html`
11. `shop_guard/models.py`
12. `task/templates/product/product_list.html`
13. `task/templates/task_list.html`
14. `theme/models.py`
15. `theme/templates/amazon_products_display.html`
16. `theme/templates/trademark_info.html`
17. `theme/templates/tro_table.html`
18. `theme/view/views_trend.py`
19. `track/templates/tracking_management.html`

## 3. 执行批次
- Batch A: 通用筛选底座（`general/static/*` + `static/*`）
- Batch B: 后端多选参数兼容（`views/api`）
- Batch C: Amazon 页面兼容
- Batch D: Task/DataReq/Track 页面兼容
- Batch E: Theme/模型层并集合并

## 4. 验收标准（每次改动后记录）
- 可搜索多选、"全部"互斥、参数拼接与后端过滤一致
- 年月/年月日弹窗打开与选择即时生效
- 日期范围自定义展开/收起与起止日期提交正确
- 搜索/重置按钮样式与交互一致
- 同事新布局/新配色不被破坏

## 5. 变更日志模板（每次改动后追加）
### [YYYY-MM-DD HH:mm] Step N
- 改动文件:
- 改动说明:
- 保留同事样式/结构点:
- 保留我方筛选逻辑点:
- 风险点:
- 验收结果:
- 下一步等待指令:

## 6. 启动日志
### [2026-02-09 00:00] Step 0 - 启动并创建计划文档
- 改动文件: `MERGE_PLAN_AND_LOG.md`
- 改动说明: 初始化完整合并计划、批次与日志机制
- 保留同事样式/结构点: 已明确以 `origin/master` 为视觉与架构主干
- 保留我方筛选逻辑点: 已锁定筛选体系与相关页面为强制保留项
- 风险点: 共同修改文件较多，需严格逐页面复核
- 验收结果: 文档已建立，可进入 Batch A
- 下一步等待指令: 等待用户“继续”

### [2026-02-09 17:08] Step 1 - 执行策略切换到 merge-test 分支
- 改动文件: `MERGE_PLAN_AND_LOG.md`
- 改动说明: 根据用户最新指令，将合并执行面从“新整合分支”切换为“直接在 merge-test 分支内兼容合并”。
- 保留同事样式/结构点: 仍以 `origin/master` 的架构和视觉改动作为吸收目标。
- 保留我方筛选逻辑点: 仍保留 `6beab03..HEAD` 的筛选体系为刚性要求。
- 风险点: 在现有分支直接合并，回滚粒度依赖本地备份分支与后续阶段性提交。
- 验收结果: 已创建本地备份分支 `backup/merge-test-before-origin-merge-20260209`。
- 下一步等待指令: 发起 `origin/master` 的 no-commit 合并并盘点真实冲突。

### [2026-02-09 17:08] Step 2 - 在 merge-test 发起 no-commit 合并
- 改动文件: `MERGE_PLAN_AND_LOG.md`
- 改动说明: 已执行 `git merge --no-commit --no-ff origin/master`，合并停留在冲突待处理状态（未提交）。
- 保留同事样式/结构点: 同事分支改动已落入工作区，等待人工筛选并融合。
- 保留我方筛选逻辑点: 仅进入冲突态，尚未进行任何覆盖性解决，筛选逻辑可按计划逐项保留。
- 风险点: git 只报出 5 个冲突文件，但另有 14 个高风险文件自动合并，必须人工复核。
- 验收结果:
  - 显式冲突文件(5):
    1. `amazon/models.py`
    2. `amazon/templates/amazon_performance_notifications.html`
    3. `shop_guard/models.py`
    4. `theme/templates/tro_table.html`
    5. `theme/view/views_trend.py`
  - 高风险自动合并文件(14):
    1. `amazon/templates/amazon_daily_check_report.html`
    2. `amazon/templates/amazon_order_management.html`
    3. `amazon/templates/amazon_shop_emails.html`
    4. `amazon/templates/dashboard.html`
    5. `amazon/templates/ranking.html`
    6. `amazon/view/views_amazon_daily_check.py`
    7. `data_req/templates/data_req/requirement_list.html`
    8. `general/templates/base/base.html`
    9. `task/templates/product/product_list.html`
    10. `task/templates/task_list.html`
    11. `theme/models.py`
    12. `theme/templates/amazon_products_display.html`
    13. `theme/templates/trademark_info.html`
    14. `track/templates/tracking_management.html`
- 下一步等待指令: 先处理第 1 个高价值冲突 `amazon/templates/amazon_performance_notifications.html`，完成后立即记录并暂停。

### [2026-02-09 17:12] Step 3 - 修复文档路径首字母缺失问题
- 改动文件: `MERGE_PLAN_AND_LOG.md`
- 改动说明: 全量重写文档，清理控制字符并修复所有路径前缀缺失（如 `theme`/`amazon`/`track`/`task`/`backup`）。
- 保留同事样式/结构点: 不涉及代码合并结果，仅修正文档准确性。
- 保留我方筛选逻辑点: 不涉及代码合并结果，仅修正文档准确性。
- 风险点: 无业务逻辑风险。
- 验收结果: 通过关键词扫描和控制字符检测，确认文档内路径前缀已全部正常。
- 下一步等待指令: 继续处理冲突文件 `amazon/templates/amazon_performance_notifications.html`。

### [2026-02-09 17:14] Step 4 - 清理复查说明中的误导性示例
- 改动文件: `MERGE_PLAN_AND_LOG.md`
- 改动说明: 将 Step 3 的验收描述改为通用表述，避免把示例字符串误读为真实路径错误。
- 保留同事样式/结构点: 不涉及代码合并结果，仅优化文档可读性。
- 保留我方筛选逻辑点: 不涉及代码合并结果，仅优化文档可读性。
- 风险点: 无。
- 验收结果: 文档路径列表和日志中的路径均已正常显示。
- 下一步等待指令: 继续处理冲突文件 `amazon/templates/amazon_performance_notifications.html`。


### [2026-02-09 17:19] Step 5 - `amazon/templates/amazon_performance_notifications.html` 按用户指令保留我方逻辑
- 改动文件: `amazon/templates/amazon_performance_notifications.html`, `MERGE_PLAN_AND_LOG.md`
- 改动说明: 对该文件执行 `--ours` 解决冲突，并标记为已解决（`git add`）。
- 保留同事样式/结构点: 保留了已自动合入的基础页面结构与样式资源接入状态；本文件冲突块内同事新增分组联动逻辑未保留。
- 保留我方筛选逻辑点: 保留我方 `dateRangeController` 日期范围统一交互与筛选读取/重置逻辑。
- 风险点: 本页将不包含同事在该文件中新增的“运营分组联动筛选”能力（后续如需可二次补回）。
- 验收结果: `amazon/templates/amazon_performance_notifications.html` 已从未解决冲突列表移除；当前剩余冲突 4 个。
- 下一步等待指令: 继续处理 `theme/templates/tro_table.html` 或 `theme/view/views_trend.py`。

### [2026-02-09 17:24] Step 6 - 解决 `theme/templates/tro_table.html` 冲突并保留通用脚本接入
- 改动文件: `theme/templates/tro_table.html`, `MERGE_PLAN_AND_LOG.md`
- 改动说明: 清理唯一冲突块，保留 `<script src="{% static 'js/general_style_1.js' %}"></script>`。
- 保留同事样式/结构点: 保留同事版本中新增的国际类/店铺字段展示与相关下拉筛选逻辑。
- 保留我方筛选逻辑点: 保留我方通用脚本接入，避免页面与全站通用筛选体系脱节。
- 风险点: 当前仅解冲突，不改变业务逻辑；该文件仍需后续页面回归验证。
- 验收结果: `theme/templates/tro_table.html` 已从未解决冲突列表移除；当前剩余冲突 3 个。
- 下一步等待指令: 继续处理 `theme/view/views_trend.py` 或 `amazon/models.py`。

### [2026-02-09 17:27] Step 7 - 解决 `theme/view/views_trend.py` 冲突（空白行冲突）
- 改动文件: `theme/view/views_trend.py`, `MERGE_PLAN_AND_LOG.md`
- 改动说明: 清理唯一冲突块（仅空白行差异），不改动任何业务逻辑。
- 保留同事样式/结构点: 不涉及前端样式结构。
- 保留我方筛选逻辑点: 不涉及筛选逻辑，保持双方既有逻辑不变。
- 风险点: 极低，仅文本冲突标记清理。
- 验收结果: `theme/view/views_trend.py` 已从未解决冲突列表移除；当前剩余冲突 2 个。
- 下一步等待指令: 继续处理 `amazon/models.py` 或 `shop_guard/models.py`。

### [2026-02-09 17:34] Step 8 - `amazon/models.py` 按用户指令以同事版本为准
- 改动文件: `amazon/models.py`, `MERGE_PLAN_AND_LOG.md`
- 改动说明: 对 `amazon/models.py` 执行 `--theirs` 并 `git add`，按用户要求移除 `AmazonListing`。
- 保留同事样式/结构点: 保留同事版本中的 `RiskKeyword` 扩展能力（如 `official` 分类与 `created_by` 字段）。
- 保留我方筛选逻辑点: 本次不涉及筛选页面逻辑。
- 风险点: 我方在该文件的 `AmazonListing` 模型能力被明确放弃。
- 验收结果: `amazon/models.py` 已从未解决冲突列表移除；当前剩余冲突 1 个。
- 下一步等待指令: 继续处理 `shop_guard/models.py`。

### [2026-02-09 17:40] Step 9 - `shop_guard/models.py` 按用户指令以同事版本为准
- 改动文件: `shop_guard/models.py`, `MERGE_PLAN_AND_LOG.md`
- 改动说明: 对 `shop_guard/models.py` 执行 `--theirs` 并 `git add`，采用同事版本。
- 保留同事样式/结构点: 完全保留同事当前文件内容。
- 保留我方筛选逻辑点: 本次不涉及筛选逻辑。
- 风险点: 同事版本当前为整文件注释代码，`shop_guard` 模型定义处于不可用状态，后续若有该 app 依赖可能影响运行。
- 验收结果: 所有冲突标记已清除，未解决冲突列表为空。
- 下一步等待指令: 进入“自动合并高风险文件”的逐文件复核阶段，或先做一次整体运行检查。

### [2026-02-09 17:45] Step 10 - 自动合并高风险页面复核 + 运行检查
- 改动文件: `MERGE_PLAN_AND_LOG.md`
- 改动说明: 对高风险自动合并页面执行关键能力复核，并运行 `python manage.py check`。
- 保留同事样式/结构点: 复核到关键页面标题/基础样式主干已体现同事改动（如 `Divi智管` 与新基座模板链路）。
- 保留我方筛选逻辑点: 复核到核心页面仍保留 `GeneralStyleDateRangeFilter`、`SearchableSelect` 与多选参数链路（含 `track`/`task`/`amazon`/`data_req`）。
- 风险点:
  - `shop_guard/models.py` 当前为同事版本（整文件注释），后续若恢复该 app 业务需再补模型定义。
  - `python manage.py check` 虽通过，但出现 `theme.themereport` 重复注册 RuntimeWarning，需后续单独清理。
- 验收结果:
  - 未解决冲突列表为空。
  - `python manage.py check` 结果：`System check identified no issues (0 silenced)`。
- 下一步等待指令: 进入“最终差异审计（逐文件确认保留策略）”或直接准备合并提交。

### [2026-02-09 17:52] Step 11 - 审计修正：`amazon/templates/amazon_performance_notifications.html` 标题统一
- 改动文件: `amazon/templates/amazon_performance_notifications.html`, `MERGE_PLAN_AND_LOG.md`
- 改动说明: 在保留该页我方筛选逻辑前提下，将页面标题从 `Overlord` 调整为 `Divi智管`，与同事全站风格保持一致。
- 保留同事样式/结构点: 页面品牌文案与主站风格对齐。
- 保留我方筛选逻辑点: 未改动该页日期范围控制器与筛选交互逻辑。
- 风险点: 低，仅文案层变更。
- 验收结果: 该页面实现“同事风格 + 我方筛选逻辑”的最小兼容修正。
- 下一步等待指令: 输出逐文件审计结论并确认是否进入提交前检查。

### [2026-02-09 17:53] Step 12 - 修正后运行检查
- 改动文件: `MERGE_PLAN_AND_LOG.md`
- 改动说明: 在 Step 11 后再次执行 `python manage.py check` 做回归验证。
- 保留同事样式/结构点: 不涉及代码改动，仅验证。
- 保留我方筛选逻辑点: 不涉及代码改动，仅验证。
- 风险点: 仍存在 `theme.themereport` 重复注册 RuntimeWarning（非本次新增）。
- 验收结果: `System check identified no issues (0 silenced)`。
- 下一步等待指令: 输出最终逐文件审计结论，或进入提交前整理。

### [2026-02-09 18:00] Step 13 - 页面级验收（URL可达性 + 关键控件静态复核）
- 改动文件: `MERGE_PLAN_AND_LOG.md`
- 改动说明: 对核心页面执行 URL 可达性检查（未登录场景）与模板关键控件复核。
- 保留同事样式/结构点: 核心页面标题/基座链路保持 `Divi智管` 风格；`base.html` 维持同事主框架。
- 保留我方筛选逻辑点: 核心页面保留 `GeneralStyleDateRangeFilter`、`SearchableSelect` 与多选参数拼接链路。
- 风险点:
  - `shop_guard/models.py` 仍为同事版本注释态，若启用该 app 业务模型需单独恢复。
  - 运行时仍有 `theme.themereport` 重复注册告警（非本轮新增）。
- 验收结果:
  - URL 可达性：以下页面均返回 `302 -> /login/?next=...`（无 500）
    1. `/amazon/dashboard/`
    2. `/amazon/amazon-order-management/`
    3. `/amazon/amazon/performance/notifications/`
    4. `/amazon/amazon/shop-emails/`
    5. `/amazon/amazon/daily-check-report/`
    6. `/task/task/list/`
    7. `/task/task/product/list/`
    8. `/data_req/`
    9. `/track/tracking-management/`
    10. `/theme/amazon/products/`
  - 页面修正：`amazon/templates/amazon_performance_notifications.html` 标题已统一为 `Divi智管`。
- 下一步等待指令: 进入“提交前整理清单”（确认要不要把 `MERGE_PLAN_AND_LOG.md` 与未跟踪文件纳入本次提交）。

### [2026-02-09 18:07] Step 14 - 按用户要求切换到 `Divi智管` 样式命名并修复全站 Logo 覆盖问题
- 改动文件:
  1. `general/templates/base/base.html`
  2. `data_req/templates/data_req/requirement_list.html`
  3. `amazon/templates/amazon_listing_management.html`
  4. `general/templates/performance_targets.html`
  5. `general/templates/management/amazon_shop_management.html`
  6. `general/templates/management/management.html`
  7. `general/templates/management/announcement_management.html`
  8. `general/templates/management/user_management.html`
  9. `general/templates/management/user_operation_management.html`
  10. `MERGE_PLAN_AND_LOG.md`
- 改动说明:
  - 恢复 `navbar-brand img` 固定尺寸约束（40x40 + max-width/max-height + object-fit），解决全站大 Logo 覆盖页面问题。
  - 将上述业务页面 `title` 中 `Overlord` 统一替换为 `Divi智管`。
- 保留同事样式/结构点: 以同事的 Divi 品牌方向为主，统一页面品牌命名。
- 保留我方筛选逻辑点: 未改动筛选交互与脚本逻辑。
- 风险点: 低，主要为样式与标题文案层修复。
- 验收结果:
  - `python manage.py check` 通过（0 issues）。
  - 业务模板中 `Overlord` 残留仅在 `demo1.html` 与 `error/500.html`（非主业务页面）。
- 下一步等待指令: 是否继续把 `demo1.html` 与 `error/500.html` 也统一为 `Divi智管`。

### [2026-02-09 18:18] Step 15 - 抛弃 `Overlord` 品牌样式资源，统一切到同事 `Divi智管` Logo 定义
- 改动文件:
  1. `general/templates/base/base.html`
  2. `general/templates/login.html`
  3. `general/templates/demo1.html`
  4. `MERGE_PLAN_AND_LOG.md`
- 改动说明:
  - 将上述公共模板中的 Logo 资源引用从 `img/y/logo.png` 统一替换为 `img/y/divi.png`（`demo1` 使用 `/static/img/y/divi.png`）。
  - 保留此前已加的 navbar Logo 尺寸约束，避免再次出现大图覆盖页面问题。
- 保留同事样式/结构点: 完全对齐同事的 `Divi智管` 品牌资源路径与展示方向。
- 保留我方筛选逻辑点: 未触碰任何筛选框、筛选弹窗、日期控件、按钮交互脚本。
- 风险点: 低，仅品牌资源路径切换；若线上静态文件未更新需执行静态资源发布流程。
- 验收结果:
  - `rg -n "img/y/logo.png" general/templates` 无匹配（已全部替换）。
  - `python manage.py check` 通过（0 issues）；保留既有 `theme.themereport` 重复注册告警（非本轮新增）。
- 下一步等待指令: 继续逐页做视觉验收，或直接进入 `merge-test` 分支提交前清单整理。

### [2026-02-09 18:29] Step 16 - 按用户指令回退为同事原始 Logo 策略（移除 40x40 约束）
- 改动文件:
  1. `general/templates/base/base.html`
  2. `general/templates/login.html`
  3. `general/templates/demo1.html`
  4. `MERGE_PLAN_AND_LOG.md`
- 改动说明:
  - 移除 `base.html` 中我方新增的 `navbar-brand img` 固定尺寸约束（`40x40`、`max-width/max-height`、`object-fit`、`display`），恢复同事样式定义。
  - Logo 引用回退为同事策略:
    1. `base.html` 使用 `{% static 'img/y/logo.png' %}`
    2. `login.html` 使用 `{% static 'img/y/logo.png' %}`
    3. `demo1.html` 使用 `/static/img/y/logo.png`
    4. `main.html` 保持同事原有 `{% static 'img/y/divi.png' %}`（未改）
- 保留同事样式/结构点: 本次完全以 `origin/master` 的 Logo 引用与尺寸逻辑为准。
- 保留我方筛选逻辑点: 未改动任何筛选框、筛选弹窗、日期控件、按钮交互逻辑。
- 风险点:
  - 由于去除固定尺寸，若 `logo.png` 源图后续被替换为异常尺寸，导航可能再次受影响；当前按同事基线执行，不额外加保护。
- 验收结果:
  - `rg` 核对通过：`base/login/demo1` 为 `logo.png`，`main` 为 `divi.png`。
  - `python manage.py check` 通过（0 issues）；`theme.themereport` 重复注册 warning 仍存在（非本轮新增）。
- 下一步等待指令: 是否继续对其余页面做“同事样式优先”的逐页视觉核对（从 `theme/templates/tro_table.html` 开始）。

### [2026-02-09 18:40] Step 17 - 以同事最新 Logo 为准，替换旧 Overlord Logo 资源与引用
- 改动文件:
  1. `general/templates/base/base.html`
  2. `general/templates/login.html`
  3. `general/templates/demo1.html`
  4. `static/img/y/logo.png`
  5. `general/static/img/y/logo.png`
  6. `MERGE_PLAN_AND_LOG.md`
- 改动说明:
  - 模板引用统一切换到同事 Logo：`base/login/demo1` 从 `logo.png` 改为 `divi.png`。
  - 资源层兜底：将 `logo.png` 文件内容替换为 `divi.png`（`static` 与 `general/static` 两处），避免遗留页面继续显示旧图。
- 保留同事样式/结构点: 统一使用同事 `Divi` 品牌资源，不保留旧 `Overlord` 视觉资源。
- 保留我方筛选逻辑点: 未改动筛选框、筛选弹窗、日期控件与按钮交互逻辑。
- 风险点: 低；若浏览器/静态资源服务存在缓存，需强刷或重新收集静态资源后生效。
- 验收结果:
  - `rg -n "img/y/logo.png|/static/img/y/logo.png" ...` 无匹配。
  - `rg -n "img/y/divi.png|/static/img/y/divi.png" ...` 已在 `base/login/demo1/main` 命中。
  - `logo.png` 与 `divi.png` 哈希一致（`static` 与 `general/static` 两套目录）。
  - `python manage.py check` 通过（0 issues）；`theme.themereport` 重复注册 warning 仍为既有问题。
- 下一步等待指令: 是否继续按同事基线逐页清理其它品牌残留（例如图标 alt 文案与页面说明文字）。

### [2026-02-09 18:50] Step 18 - 按用户指正重新拉取远程 `logo.png` 并恢复同事引用策略
- 改动文件:
  1. `general/static/img/y/logo.png`
  2. `static/img/y/logo.png`
  3. `general/templates/base/base.html`
  4. `general/templates/login.html`
  5. `general/templates/demo1.html`
  6. `MERGE_PLAN_AND_LOG.md`
- 改动说明:
  - 先执行 `git fetch origin`，确认远程 `origin/master` 中 `logo.png` 与 `divi.png` 为不同文件（不同 blob）。
  - 使用 `git checkout origin/master -- general/static/img/y/logo.png static/img/y/logo.png` 恢复两处 `logo.png` 为远程同事版本。
  - 将模板引用回归同事路径：
    1. `base.html` -> `{% static 'img/y/logo.png' %}`
    2. `login.html` -> `{% static 'img/y/logo.png' %}`
    3. `demo1.html` -> `/static/img/y/logo.png`
  - `main.html` 保持同事原有 `divi.png`（未改）。
- 保留同事样式/结构点: 以远程仓库同事 `general/static/img/y/logo.png` 为最终基线。
- 保留我方筛选逻辑点: 未触碰筛选框、筛选弹窗、日期控件、按钮交互逻辑。
- 风险点: 若浏览器静态缓存未刷新，页面可能暂时显示旧图，需强刷。
- 验收结果:
  - 本地 `logo.png` 哈希恢复为 `16a7ca91...`，与 `origin/master` 一致。
  - 本地 `divi.png` 哈希为 `e4371c66...`，与 `origin/master` 一致。
  - 模板引用核对通过：`base/login/demo1` 使用 `logo.png`，`main` 使用 `divi.png`。
  - `python manage.py check` 通过（0 issues）；`theme.themereport` 重复注册 warning 为既有问题。
- 下一步等待指令: 是否继续逐页排查其它静态资源是否也存在“引用路径正确但文件内容漂移”的情况。

### [2026-02-10 08:52] Step 19 - 从 `amazon_products_display.html` 开始做“同事样式优先 + 我方筛选兼容”
- 改动文件:
  1. `theme/templates/amazon_products_display.html`
  2. `MERGE_PLAN_AND_LOG.md`
- 对比与排查结论:
  - `general/templates/base/base_z.html` 与 `origin/master` 对比无差异（同事基座样式一致）。
  - 页面异常根因定位为样式层冲突：`general_style_1.css` 含全局 `.table/.table-container` 与固定列规则，会覆盖该页面（同事样式基线为 `amazon_css.css`）并导致表格列展示异常风险（含图片列显示不完整）。
- 回退与兼容策略:
  - 回退上一轮对表格列的强制覆盖补丁（不再用大范围 `!important` 改表格固定列逻辑）。
  - 移除本页 `general_style_1.css` 全局引用，恢复“同事页面整体样式优先”。
  - 为保留我方筛选交互与样式，仅在本页内联必要样式：
    1. `gs-date-range-custom-group` 显隐规则
    2. `searchable-select` 组件样式（触发器、下拉面板、搜索框、选项态）
  - 继续保留我方筛选逻辑脚本：`searchable-select.js` + `general_style_1.js` + `GeneralStyleDateRangeFilter` 初始化逻辑。
- 保留同事样式/结构点: 表格、卡片、按钮主视觉继续以 `amazon_css.css` 为基线。
- 保留我方筛选逻辑点: 日期范围筛选、可搜索下拉、自定义日期显隐与筛选参数收集逻辑保留。
- 验收结果:
  - `python manage.py check` 通过（0 issues）。
  - `theme.themereport` 重复注册 warning 为既有问题（非本轮新增）。
- 下一步等待指令: 是否继续在该页面做一轮“图片列视觉微调”（仅限本页小范围，不改全局 CSS）。

### [2026-02-10 09:02] Step 20 - 逐页兼容（第1页）：`theme/templates/trademark_info.html`
- 改动文件:
  1. `theme/templates/trademark_info.html`
  2. `MERGE_PLAN_AND_LOG.md`
- 改动说明:
  - 该页当前未使用 `SearchableSelect`/`GeneralStyleDateRangeFilter` 交互，保留 `general_style_1.css/js` 会引入全局表格/筛选样式覆盖风险。
  - 按“同事整体样式优先”回归：移除以下注入
    1. `general_style_1.css`
    2. `searchable-select.js`
    3. `general_style_1.js`
  - 页面保留 `amazon_css.css` 与本页自定义样式、现有按钮/筛选逻辑不变。
- 保留同事样式/结构点: 回归到同事该页基线（与 `origin/master` 的资源依赖一致）。
- 保留我方筛选逻辑点: 本页筛选仍为输入框+按钮逻辑，行为保持不变。
- 风险点: 低；主要为移除未使用的全局依赖。
- 验收结果:
  - `rg` 核对：`trademark_info.html` 内已无 `general_style_1.*` 与 `searchable-select.js` 引用。
  - `python manage.py check` 通过（0 issues）。
- 下一步等待指令: 请你确认这一页视觉与交互是否正常；确认后继续第2页（建议 `track/templates/tracking_management.html`，该页含日期筛选组件，需采用“仅保留筛选组件样式”策略）。

### [2026-02-10 09:12] Step 21 - 逐页兼容（第2页）：`track/templates/tracking_management.html`
- 改动文件:
  1. `track/templates/tracking_management.html`
  2. `MERGE_PLAN_AND_LOG.md`
- 改动说明:
  - 该页已接入我方新版筛选交互（`SearchableSelect` + `GeneralStyleDateRangeFilter`），但引入 `general_style_1.css` 会带入全局 `.table/.table-container` 规则，存在覆盖同事页面样式风险。
  - 采用与第1页一致的兼容策略：
    1. 移除 `general_style_1.css` 引用（保留同事 `amazon_css.css + tracking_management.css` 主样式）。
    2. 保留 `searchable-select.js` 与 `general_style_1.js`（保证筛选交互逻辑继续可用）。
    3. 在页面内联“仅筛选组件必需样式”：
       - `gs-date-range-custom-group` 显隐
       - `searchable-select`（单选/多选、搜索、标签、清除按钮）样式
- 保留同事样式/结构点: 表格、统计卡片、弹窗、按钮主视觉由 `tracking_management.css` 继续主导。
- 保留我方筛选逻辑点: 日期范围、多选筛选（物流商/工厂/运营分组/运营姓名）与对应 JS 逻辑全部保留。
- 风险点: 低；主要为样式拆分，逻辑未改。
- 验收结果:
  - `python manage.py check` 通过（0 issues）。
  - 页面内仍保留 `searchable-select.js` 与 `general_style_1.js` 引用，`general_style_1.css` 已移除。
- 下一步等待指令: 请你确认该页样式与筛选交互是否正常；确认后继续第3页（建议 `task/templates/task_list.html`）。

### [2026-02-10 09:22] Step 22 - 逐页兼容（第3页）：`task/templates/task_list.html`
- 改动文件:
  1. `task/templates/task_list.html`
  2. `MERGE_PLAN_AND_LOG.md`
- 改动说明:
  - 该页已接入我方新版筛选（状态/创建人多选 + 日期范围控件），但 `general_style_1.css` 会带入全局样式覆盖同事页面主样式。
  - 采用一致策略：
    1. 移除 `general_style_1.css` 引用。
    2. 保留 `searchable-select.js` 与 `general_style_1.js`（交互逻辑继续可用）。
    3. 在本页内联筛选组件必需样式：
       - `gs-date-range-custom-group` 显隐规则
       - `searchable-select` 单选/多选、搜索、标签、清除按钮样式
- 保留同事样式/结构点: 页面卡片、表格、按钮、弹窗继续由同页样式与同事基线样式主导。
- 保留我方筛选逻辑点: 状态/创建人多选、日期范围及自定义日期逻辑保留。
- 风险点: 低；样式拆分不改业务逻辑。
- 验收结果:
  - `python manage.py check` 通过（0 issues）。
  - 页面仍保留 `searchable-select.js` 与 `general_style_1.js`，`general_style_1.css` 已移除。
- 下一步等待指令: 请你确认该页后，继续第4页（建议 `data_req/templates/data_req/requirement_list.html`）。

### [2026-02-10 09:32] Step 23 - 逐页兼容（第4页）：`data_req/templates/data_req/requirement_list.html`
- 改动文件:
  1. `data_req/templates/data_req/requirement_list.html`
  2. `MERGE_PLAN_AND_LOG.md`
- 改动说明:
  - 该页主体样式来自 `tracking_management.css`，新增筛选交互只依赖 `searchable-select.js + general_style_1.js`。
  - 为对齐同事页面主样式并避免全局覆盖，移除 `general_style_1.css`。
  - 页面内联补齐筛选组件必需样式（`searchable-select` 单选样式），保留完成时间筛选新交互。
- 保留同事样式/结构点: 卡片/表格/弹窗/按钮继续由同页和同事基线样式主导。
- 保留我方筛选逻辑点: `timeFilter` 的 `GeneralStyleDateRangeFilter` 交互保留。
- 风险点: 低；仅样式依赖拆分。
- 验收结果:
  - `python manage.py check` 通过（0 issues）。
  - 页面保留 `searchable-select.js` 与 `general_style_1.js`，`general_style_1.css` 已移除。
- 下一步等待指令: 请确认该页后继续第5页（建议 `amazon/templates/dashboard.html`）。

### [2026-02-10 10:05] Step 24 - 回退上一步（`data_req/templates/data_req/requirement_list.html`）
- 改动文件:
  1. `data_req/templates/data_req/requirement_list.html`
  2. `MERGE_PLAN_AND_LOG.md`
- 回退说明:
  - 按你的最新要求，回退 Step 23 对该页的改动（撤销“移除 `general_style_1.css` + 注入筛选组件内联样式”）。
  - 当前以“同事整体新样式优先 + 你的筛选框/筛选弹窗/按钮逻辑保留”为统一原则，不再把 `general_style_1.css/js` 作为必保留项。
- 当前状态:
  - `data_req/templates/data_req/requirement_list.html` 已回退到 Step 23 之前状态。
  - 后续逐页处理时，将按页面实际依赖决定是否保留 `general_style_1.css/js`，并优先贴合同事新样式。
- 下一步:
  - 继续逐页排查并改造（你逐页确认）。

### [2026-02-10 10:22] Step 25 - 逐页兼容：`yuser/templates/assessment_summary.html`
- 改动文件:
  1. `yuser/templates/assessment_summary.html`
  2. `MERGE_PLAN_AND_LOG.md`
- 改动说明:
  - 对比同事基线后，该页不依赖 `SearchableSelect/DateRange` 组件，且同事版本未引入 `general_style_1.css/js`。
  - 移除该页的 `general_style_1.css` 与 `general_style_1.js` 引用，避免全局样式/脚本额外覆盖。
  - 保留该页原有筛选与导出逻辑（`monthFilter` 回车提交、`exportExcel` 提交逻辑不变）。
- 保留同事样式/结构点: 与同事该页资源依赖对齐（不再额外注入 `general_style_1.*`）。
- 保留我方筛选逻辑点: 月份筛选与按钮行为保持不变（使用原生 `<input type="month">`）。
- 风险点: 低；仅移除未必要依赖。
- 下一步等待指令: 请你确认该页视觉和交互是否正常；确认后继续下一页。

### [2026-02-10 10:28] Step 26 - 修正 Step 25（`yuser/templates/assessment_summary.html`）
- 修正原因:
  - 复查发现该页使用 `ym-picker`（`data-ym-picker` + `readonly` 输入），月份选择交互依赖 `general_style_1.js` 中的 `GeneralStyleYearMonthPicker`。
  - 若移除该 JS，会导致月份选择器不可用（交互回归）。
- 修正内容:
  1. 恢复 `general_style_1.js` 引用。
  2. 恢复 `GeneralStyleYearMonthPicker.initAll(document)` 初始化。
  3. 保持 `general_style_1.css` 移除不变（减少全局样式覆盖）。
- 当前结论:
  - 该页采用“同事样式优先（去全局样式）+ 我方筛选交互保留（保留必要 JS）”的兼容方案。


### [2026-02-10 10:42] Step 27 - 修复 assessment_summary 按钮显示异常
- 改动文件:
  1. yuser/templates/assessment_summary.html
  2. MERGE_PLAN_AND_LOG.md
- 问题现象:
  - 页面按钮显示异常（含筛选区按钮与月份选择器相关按钮）。
- 根因分析:
  - 该页使用自定义 ym-picker（data-ym-picker + GeneralStyleYearMonthPicker），移除 general_style_1.css 后，ym-picker 相关按钮/弹层样式失去来源，出现浏览器默认按钮样式。
  - 同时 btn 缺少 inline-flex 对齐，图标按钮在部分布局下视觉不一致。
- 修复措施:
  1. 在页面内联补齐 ym-picker 最小样式（输入框、左右切换/清空按钮、年份头部、月份网格、弹层）。
  2. 补齐 btn 的对齐样式：display:inline-flex、align-items:center、justify-content:center、gap、line-height、text-decoration。
  3. 保持 general_style_1.css 继续移除，仅保留 general_style_1.js 用于月份交互逻辑初始化。
- 兼容策略结果:
  - 同事整体样式不被全局 CSS 覆盖。
  - 你的筛选控件与按钮交互视觉恢复。

### [2026-02-10 11:05] Step 28 - 逐页兼容：`yuser/templates/assessment_management_list.html`
- 改动文件:
  1. `yuser/templates/assessment_management_list.html`
  2. `MERGE_PLAN_AND_LOG.md`
- 改动说明:
  - 该页已切换到我方新版筛选控件（`searchable-select`）与月份选择器（`ym-picker`），但这些样式此前依赖 `general_style_1.css`。
  - 为对齐同事整体样式并避免全局覆盖，移除页面 `general_style_1.css`。
  - 在页面内联补齐最小必需样式：
    1. `btn` 对齐样式（`inline-flex`、图标间距、文本对齐）
    2. `ym-picker` 样式（输入框、左右切换/清空按钮、年份与月份网格、弹层）
    3. `searchable-select` 样式（触发器、下拉面板、搜索框、选项、标签、多选勾选）
  - 保留 `searchable-select.js` 与 `general_style_1.js`，确保筛选与月份交互逻辑不变。
- 保留同事样式/结构点:
  - 页面主框架、卡片、表格、弹窗仍由本页原有样式和同事基线主导，不再由全局 `general_style_1.css` 覆盖。
- 保留我方筛选逻辑点:
  - 员工/类型/状态筛选（含搜索下拉）和 `GeneralStyleYearMonthPicker` 交互全部保留。
- 验收结果:
  - `python manage.py check` 通过（0 issues）。
- 下一步等待指令:
  - 请你确认该页筛选、按钮和弹窗显示是否正常；确认后继续下一页（建议 `general/templates/performance_targets.html`）。

### [2026-02-10 11:18] Step 29 - 修复筛选框提示词发灰
- 改动文件:
  1. yuser/templates/assessment_management_list.html`n  2. 	ask/templates/task_list.html`n  3. 	rack/templates/tracking_management.html`n  4. MERGE_PLAN_AND_LOG.md`n- 问题现象:
  - 筛选框提示词显示偏灰，视觉上像消失。
- 修复措施:
  - 将 searchable-select 占位词样式调整为更可读：
    1. color: var(--text-primary)`n    2. ont-weight: 500`n    3. opacity: 0.82`n- 说明:
  - 仅调整视觉，不改筛选逻辑。
- 验收结果:
  - python manage.py check 通过（0 issues）。
### [2026-02-10 11:30] Step 30 - 按指令回退
- 回退内容:
  1. `yuser/templates/assessment_summary.html`：回退到本轮第一次改动前。
  2. `yuser/templates/assessment_management_list.html`：回退到本轮第一次改动前。
  3. `task/templates/task_list.html`：回退上次提示词颜色改动。
  4. `track/templates/tracking_management.html`：回退上次提示词颜色改动。
- 结果:
  - `assessment` 两页当前已无本轮改动差异。
  - `task/track` 提示词样式已恢复为回退前状态。
